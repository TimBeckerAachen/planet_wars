services:
  # Backend Service
  - type: web
    name: planetwars-backend
    runtime: docker
    repo: https://github.com/TimBeckerAachen/planet_wars
    branch: main
    rootDir: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: planetwars-db
          property: connectionString
      - key: PORT
        value: 8000
    healthCheckPath: /health
    autoDeploy: false

  # Frontend Service
  - type: web
    name: planetwars-frontend
    runtime: docker
    repo: https://github.com/TimBeckerAachen/planet_wars
    branch: main
    rootDir: frontend
    plan: free
    envVars:

      - key: BACKEND_HOST
        fromService:
          type: web
          name: planetwars-backend
          property: host
      - key: BACKEND_PORT
        fromService:
          type: web
          name: planetwars-backend
          property: port

    autoDeploy: false

databases:
  - name: planetwars-db
    databaseName: planetwars
    user: planetwars_user
    plan: free
